// The scanner will make use of libconfig for configuration files.
// This file will be used for testing purposes and as example.
// It will NOT be used in production.


// Run ever n seconds

microseconds = 5

directories = {
              folders = ( {   name = "Big Secret folder";
              path = "/home/user2/topSecret/";
              recursive = 0;
              },
              {   name = "Ransomware honeypot";
              path = "/user/home/AAA/";
              recursive = 0;
              },

              {   name = "User files";
              path= "/home//user2/documents/";
              recursive = 1; }
              );
};

whitelist = {
            files = ( {   name = "normal document";
            path = "/home/user2/documents/";
            file = "normal.otd"
            recursive = 0;
            },

            {   name = "fast changing folder";
            file = ""
            path= "/home//user2/topSecret/liveStream";
            recursive = 1; }
            );
};